version: '3.7' 
services:
  dbms:
    image: cyverse/irods-test-postgres
    restart: always

  irods:
    image: "cyverse/irods-test:v$IRODS_VER"
    hostname: "$IRODS_CONF_HOST"
    restart: always
    ports:
      - 1247:1247
    depends_on:
      - dbms